# Monei · Vista (demo HTML autónoma)

Rama `vista`: solo `index.html` + `vercel.json`. Es la muestra Buddy/MonAI con el pase HIG y la capa
"aprende" (tipo de gasto → presupuesto con memoria, todo guardado en el dispositivo). No toca `master`
ni la app de producción. Se regenera con `build_hig.py` + `build_learn.py` (brain, projects/).
